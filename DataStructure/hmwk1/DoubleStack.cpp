#include<iostream>
#include"DoubleStack.hpp"

using namespace std;

int main()
{
    int pageHandler = 1;
    twStack s;
    InitStack(s);

    if(isStackExist(s))
    {
        while(pageHandler != 0)
        {
            
        }
        return 0;
    }
    else
    {
        return 0;
    }
}